# Core runtime dependencies from the current meetflow environment.
requests==2.33.1

# Explicit transitive dependencies currently installed in meetflow.
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
packaging==26.0
urllib3==2.6.3

# Optional local RAG indexing dependencies.
# They are used by core/knowledge.py, but are not installed in the current
# meetflow environment.
# chromadb>=0.5.0
# sentence-transformers>=3.0.0

# Optional Feishu SDK callback dependency.
# Keep this in the isolated .venv-lark-oapi environment created by:
# python scripts/setup_lark_oapi_venv.py --recreate
# lark-oapi==1.4.0
